export interface CurrentUserInterface {
    username: string;
    email: string;
    image: string | null;
    bio: string | null;
}    
// Tokens removing token
// accessToken: string;
// refreshToken: string;